"""Base service class for all domain services."""


class BaseService:
    """Base class for all services.

    Import constraints are enforced statically by scripts/audit_imports.py
    which runs in CI.
    """
