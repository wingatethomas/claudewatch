"""Atomic UI widgets — label, button, toggle, card factories.

All widgets follow the zero-frame convention: they start with frame (0, 0, 0, 0).
The layout system (VStack) or the caller is responsible for setting the actual
position and size. This prevents intrinsic sizing from conflicting with layout.
"""
