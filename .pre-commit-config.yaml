repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.6
    hooks:
      - id: ruff
        args: [--fix, --config=ruff.toml]
      - id: ruff-format
        args: [--config=ruff.toml]
